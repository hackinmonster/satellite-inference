services:
  redis:
    image: redis:7-alpine
    ports:
      - "6381:6379"

  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_DB: 0
    depends_on:
      - redis
